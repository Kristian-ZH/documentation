structure:
- name: blog
  nodesSelector:
    path: https://github.com/dimitar-kostadinov/documentation/tree/remove_duplicates/website/blog
  properties:
    index: true
    frontmatter:
      title: Blogs
      layout: blog-list
      aggregate: true
- name: adopter
  nodes:
  - source: https://github.com/dimitar-kostadinov/documentation/blob/remove_duplicates/website/adopter/_index.md
    properties:
      frontmatter:
        title: Adopters
        url: /adopter
- name: community
  nodes:
  - source: https://github.com/dimitar-kostadinov/documentation/blob/remove_duplicates/website/community/_index.md
    properties:
      title: Community
      url: /community
{{- $vers := Split .versions "," -}}
{{- range $i, $version := $vers -}}
{{- if eq $i 0  }}
- name: docs
{{- else }}
- name: {{$version}}
{{- end }}
  nodesSelector:
    path: https://github.com/dimitar-kostadinov/documentation/blob/remove_duplicates/.docforge/documentation.yaml
  links:
    rewrites:
      dimitar-kostadinov/documentation/(tree|blob|raw):
        version: {{$version}}
      dimitar-kostadinov/gardener/(tree|blob|raw):
        version: {{$version}}
{{- end }}
links:
  downloads:
    scope:
      dimitar-kostadinov/documentation/(blob|raw)/(.*)/website: ~
